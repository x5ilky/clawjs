import "intrin"

impl Runtime for int {
    fn sizeof() int {
        return 1
    }
    
    fn to_scratch_value(self: Self) JsObject {
        obj := $.create_js_object()

        return obj
    }
}
import "std"

cat := make_sprite("Cat", false)
pop := make_costume("svg", "pop.svg")
background := make_costume("svg", "background.svg")

i := make_number!()
list := make_list!<number!>()

stage:add_costume(background)

cat:add_costume(pop)
cat:on_flag(!{
    i = 0
    forever(!{
        list:push_single(i)
        i += 1
    })
})
